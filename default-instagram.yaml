# yq ea -iI 4 'select(fi==0) *? select(fi==1)' config.yaml ../default_settings/default-instagram.yaml

# Application service host/registration related details.
# Changing these values requires regeneration of the registration.
appservice:
    # The full URI to the database. SQLite and Postgres are supported.
    # Format examples:
    #   SQLite:   sqlite:///filename.db
    #   Postgres: postgres://username:password@hostname/dbname
    database: postgres://instagram_user:aex3aenainaiphoo6ohmah0sifa8cheebaiThahk3ooHuixaiNg4Ufaipheo1bee@localhost:5432/instagram
# Bridge config
bridge:
    # Displayname template for Instagram users.
    # {displayname} is replaced with the display name of the Instagram user.
    # {username} is replaced with the username of the Instagram user.
    displayname_template: {displayname} (IG)
    # Displayname template for 1:1 chat portals. Same variables as displayname_template.
    private_chat_name_template: {displayname} (IG)
    # Localpart template of MXIDs for Instagram users.
    # {userid} is replaced with the user ID of the Instagram user.
    username_template: ig_{userid}
